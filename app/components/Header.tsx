'use client';

import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="py-12 text-center">
      <h1 className="text-6xl font-light  text-title-color">SPLITTER</h1>
    </header>
  );
};

export default Header;
